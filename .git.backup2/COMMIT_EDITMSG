fix(ui+dev): gazette pagination, full-width sticky bar, drop stale seed step

UI:
- Restyle Bootstrap pagination to gazette theme (cream/burgundy, square corners, Inter font)
- Sticky save bar: compact like top nav, breaks out to full viewport width with viewport margin trick

Dev:
- dev-native.bat: drop duplicate db:seed call (NativeAppServiceProvider seeds conditionally if empty),
  fixing UniqueConstraintViolationException on subsequent runs

Security:
- Untrack .env.bak (created by dev-native.bat during .env swap) and add to .gitignore
